#include<stdio.h>
void main()
{
    int a,b,c;
    printf("Enter year");
    scanf("%d",&a);

    if(a%4==0)
    {
        else
            if(a%400==0)
        {
            else
            if(a%100!=0)
            {
                printf("leap year");
            }
        }
    }
}

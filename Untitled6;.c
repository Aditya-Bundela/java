#include<stdio.h>
void main()
{
    int a;
    printf("Enter year");
    scanf("%d",&a);

    while(a%400==0)
    {
      if(a%4==0)
      (
          else if(a%100!=0)
          {
            printf("leap year");
          }
        else
        {
            printf("not a leap year");

    }
}

